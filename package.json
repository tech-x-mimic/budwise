{
  "name": "budwise",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "node-sass scss/index.scss build/budwise.css",
    "prefix": "npm run build && postcss --no-map --use autoprefixer -o build/budwise.main.css build/budwise.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "budwise.css",
    "scss"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.2.4",
    "node-sass": "^5.0.0",
    "postcss": "^8.2.6",
    "postcss-cli": "^8.3.1"
  },
  "dependencies": {}
}
